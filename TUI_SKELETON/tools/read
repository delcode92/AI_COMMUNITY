#!/bin/bash
# Read file contents tool
# Usage: read <filepath>

FILE="$1"

if [ -z "$FILE" ]; then
    echo "Error: No file path provided" >&2
    echo "Usage: read <filepath>" >&2
    exit 1
fi

if [ ! -f "$FILE" ]; then
    echo "Error: File not found: $FILE" >&2
    exit 1
fi

cat "$FILE"